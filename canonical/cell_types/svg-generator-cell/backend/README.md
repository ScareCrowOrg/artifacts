# SVG Generator Cell – Backend

## Purpose

Python backend for the **SVG Generator Cell** — generates SVG visualizations from text prompts using LLM services.

## Content Index

| Directory | Description |
|-----------|-------------|
| [`scripts/`](./scripts/) | `main.py` — entry point; calls LLM to generate SVG markup from a text prompt; validates and sanitizes output SVG |
| [`tests/`](./tests/) | `test_main.py` — backend unit tests |

## Related

- [`../`](../) — SVG Generator Cell root
