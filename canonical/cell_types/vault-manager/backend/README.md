# Vault Manager Cell – Backend

## Purpose

Python backend for the **Vault Manager Cell** — encrypted secret CRUD operations for the Launcher vault.

## Content Index

| Directory | Description |
|-----------|-------------|
| [`scripts/`](./scripts/) | `main.py` — `list`, `create`, `rotate`, `delete`, `audit` actions; AES-256-GCM encryption, masked output |
| [`tests/`](./tests/) | `test_main.py` — backend unit tests |

## Related

- [`../`](../) — Vault Manager Cell root
