# Settings Panel Cell – Frontend Components

## Purpose

Per-tab settings form components for the Settings Panel Cell.

## Content Index

| File | Description |
|------|-------------|
| [`AdminSettings.vue`](./AdminSettings.vue) | Admin-only settings form — system configuration, feature flags; requires `settings:admin` |
| [`LayoutSettings.vue`](./LayoutSettings.vue) | Layout preferences — sidebar position, panel sizes, workspace density |
| [`ThemeSettings.vue`](./ThemeSettings.vue) | Theme settings — light/dark mode, color scheme, font size |
| [`UserSettings.vue`](./UserSettings.vue) | User preferences — display name, language, notification settings |

## Related

- [`../`](../) — Settings Panel Cell frontend root
